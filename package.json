{
  "name": "spring-msa-frontend-common",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": "24.19.0",
    "pnpm": "10.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "publish:packages": "pnpm -r publish --access public --no-git-checks"
  }
}
